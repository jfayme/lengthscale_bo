# lsab embedding cache (python -m lsab.featurize): large and regenerable
embeddings/
# the old tree's embedding cache: large and regenerable
gollum_out/
__pycache__/
# sweep outputs (python -m lsab.sweep): results and logs, shared outside git
runs/
# local settings override (machine-specific: device, weights, log paths)
.env.toml
# log files written by logging_config.setup_logging
*.log
